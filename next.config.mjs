/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'github.com',
          'avatars0.githubusercontent.com',
          'www.ics.hawaii.edu',
          'static.voices.com',
        ],
    },
};

export default nextConfig;
